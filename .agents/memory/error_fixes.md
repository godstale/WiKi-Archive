# Error Fixes & Gotchas (Memory)

이 파일은 에이전트가 작업 중 겪은 시행착오와 해결 방법을 기록하여 반복되는 실수를 방지하는 '경험 지식고'입니다.

## 1. 일반적인 실수 (Gotchas)
- **서브모듈 경로**: 모든 스킬은 `.agents/skills/`에 있어야 함. 루트에 직접 설치 금지.
- **Git Push**: `WiKi-Hub` 실행 전 원격지 연결 확인 필수.

## 2. 오류 및 해결 이력
- **2026-05-10**: `mkdir` 실행 시 폴더가 이미 존재할 경우 발생하는 에러 처리 (PowerShell 호환성 고려 필요).
- **2026-05-10**: `.claude/rules`에서 `.agents/rules`로 규칙 저장소 통합 및 에이전트 중립화 수행.
